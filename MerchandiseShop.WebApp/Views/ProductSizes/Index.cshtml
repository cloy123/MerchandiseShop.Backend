@model MerchandiseShop.Application.ProductSizes.Queries.GetProductSizeList.ProductSizeListVm

@{
    ViewData["Title"] = "Index";
}


<h1>Размеры</h1>

<p>
    <a asp-action="Create">Добавить размер</a>
</p>
<div>
    <a asp-action="Index" asp-controller="Products">Назад</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                Название
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProductSizes)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Изменить</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>